/**
 * Centralized Role Constants.
 * Phase 5.5 PRD: Standardize roles to strictly lowercase string values to prevent Logical Drift.
 */
const VALID_ROLES = [
    'admin',
    'branch_manager',
    'staff'
];

module.exports = VALID_ROLES;
